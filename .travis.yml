language: ruby
rvm:
 - 2.2.6
branches:
  - master
deploy:
#  provider: pages
  skip_cleanup: true
#  github_token: $GITHUB_TOKEN
#  on:
#    branch: master
  provider: heroku
  api_key:
    secure: $HEROKU_API_KEY
  app: my-resushi-app
  on: gh-pages
	

